// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:e7c780e8e16eb195
#![allow(unexpected_cfgs)]

use anchor_lang::prelude::*;

mod instructions;
pub mod state;
pub mod guards;
pub mod ref_impls;

declare_id!("11111111111111111111111111111111");

#[program]
pub mod haircutconservation {
    use super::*;

    pub fn convert(ctx: Context<Convert>, matured: u128, h: u128) -> Result<()> {
        ctx.accounts.handler(matured, h)
    }

}

// `#[derive(Accounts)]` structs live at the crate root so the
// Anchor `#[program]` macro can resolve them via `crate::*`.
// The handler impl blocks live next to the (always-regenerated)
// guard module in `instructions/<name>.rs`.
use crate::state::*;

#[derive(Accounts)]
pub struct Convert {
}
// ---- END GENERATED ----
