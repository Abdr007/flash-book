// ---- GENERATED BY QEDGEN ----
//
// Proptest harnesses — property-based testing for the spec's state machine.
// Tier 1 of the verification waterfall: finds counterexamples in milliseconds.
//
//   Proptest: random testing, fast counterexamples (~100ms)
//   Kani:     bounded model checking, exhaustive within bounds (~5-30s)
//   Lean:     mathematical proof, universal guarantees (minutes-hours)
//
// To run:  cargo test --test proptest
// Deep:    PROPTEST_CASES=10000 cargo test --test proptest
// ---- ---- ---- ---- ---- ---- ---- ---- ---- ---- ---- ---- ---- ---- ----

use proptest::prelude::*;

const H_DENOM: u32 = 1000000000;

#[derive(Debug, Clone, Copy)]
struct State {
    credited: u128,
}

/// Proptest strategy for generating arbitrary State values.
prop_compose! {
    fn arb_state()(
        credited in 0u128..=u128::MAX,
    ) -> State {
        State {
            credited,
        }
    }
}

/// Boundary-biased strategy for guard rejection tests.
prop_compose! {
    fn arb_boundary_state()(
        credited in prop_oneof![0u128..=3u128, (u128::MAX - 3)..=u128::MAX],
    ) -> State {
        State {
            credited,
        }
    }
}

fn convert(s: &mut State, matured: u128, h: u128) -> bool {
    if !((h <= 1000000000)) {
        return false;
    }
    s.credited = (haircut_credit (matured) (h));
    true
}

proptest! {
    #![proptest_config(ProptestConfig { max_global_rejects: 65536, ..ProptestConfig::with_cases(256) })]
    #[test]
    fn convert_rejects_invalid(s in arb_boundary_state(), matured in prop_oneof![0u128..=3u128, (u128::MAX - 3)..=u128::MAX], h in prop_oneof![0u128..=3u128, (u128::MAX - 3)..=u128::MAX]) {
        let mut s = s;
        prop_assume!(!((h <= 1000000000)));
        prop_assert!(!convert(&mut s, matured, h),
            "convert must reject when guard is violated");
    }
}

